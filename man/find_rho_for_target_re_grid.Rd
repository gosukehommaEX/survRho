% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_rho_for_target_re_grid.R
\name{find_rho_for_target_re_grid}
\alias{find_rho_for_target_re_grid}
\title{Find Rho for Target Relative Efficiency Over a Grid of Parameter Combinations}
\usage{
find_rho_for_target_re_grid(
  HR,
  hazard_c,
  f,
  d,
  a_k,
  omega_k,
  r,
  target_re,
  metric = "n",
  alpha,
  power,
  time_unit = "months",
  rho_range = c(0.1, 5),
  tolerance = 1e-06,
  max_iter = 1000,
  formula = "Schoenfeld",
  verbose = FALSE
)
}
\arguments{
\item{HR}{A numeric vector of hazard ratios under the alternative hypothesis.}

\item{hazard_c}{A numeric vector of hazard rates for the control group
(per time unit).}

\item{f}{A numeric vector of follow-up times after completion of accrual.}

\item{d}{A numeric vector of dropout rates (annual probability) applied to
both the experimental and control groups (d_t = d_c = d).}

\item{a_k}{A numeric vector of accrual time interval boundaries.}

\item{omega_k}{A numeric vector of accrual intensities (patients per time
unit) for each accrual interval.}

\item{r}{A numeric vector of allocation ratios to the experimental group
(T:C = r:1).}

\item{target_re}{A numeric vector of target relative efficiency values.}

\item{metric}{A character string specifying the outcome for which the
target RE is sought. One of "n" (total sample size),
"accrual_duration" (accrual duration a_K), or "tau" (total trial
duration). Default is "n".}

\item{alpha}{A one-sided level of significance.}

\item{power}{A target power.}

\item{time_unit}{A character string specifying the time unit. One of
"months" (default), "weeks", or "years".}

\item{rho_range}{A numeric vector of length 2 giving the search range for
rho. Default is c(0.1, 5).}

\item{tolerance}{A numeric value specifying the tolerance for the root
finder. Default is 1e-6.}

\item{max_iter}{A maximum number of iterations for the root finder.
Default is 1000.}

\item{formula}{A character string specifying the formula used to calculate
the required number of events. One of "Schoenfeld" (default) or "Freedman".}

\item{verbose}{Logical. If TRUE, progress messages are printed for grids
with more than 50 combinations, and a summary is printed at the end.
Default is FALSE.}
}
\value{
A data frame with one row per parameter combination, with columns:
\describe{
  \item{HR}{Hazard ratio}
  \item{hazard_c}{Control-group hazard rate}
  \item{mst_c}{Median survival time for the control group
    (= log(2) / hazard_c)}
  \item{f}{Follow-up time}
  \item{d}{Dropout rate (annual probability)}
  \item{r}{Allocation ratio (experimental:control)}
  \item{target_re}{Target relative efficiency}
  \item{metric}{Outcome used for optimization}
  \item{rho}{Rho value that achieves the target RE}
  \item{achieved_re}{Relative efficiency actually achieved at rho}
  \item{converged}{Logical indicating whether the root finder converged}
  \item{objective}{Absolute difference |achieved_re - target_re|}
  \item{iterations}{Number of iterations used by the root finder}
  \item{status}{"Success", "Failed", or "Error"}
  \item{error_message}{Error message if status = "Error", otherwise NA}
}
}
\description{
Searches for the accrual rate multiplier rho that achieves a specified
target relative efficiency for each combination of trial design parameters
(hazard ratio, control-group hazard rate, follow-up period, dropout rate,
allocation ratio, and target RE). For each combination,
\code{\link{find_rho_for_target_re}} is called internally.
}
\details{
The total number of scenarios evaluated is
length(HR) x length(hazard_c) x length(f) x length(d) x length(r) x length(target_re).
Failures in individual combinations (e.g., target_re outside the achievable
range) are caught and reported in the status column; the remaining
combinations are still processed.
}
\note{
This function requires \code{\link{find_rho_for_target_re}}.
}
\examples{
\dontrun{
results <- find_rho_for_target_re_grid(
  HR = c(0.6, 0.7, 0.8),
  hazard_c = c(log(2) / 6, log(2) / 12, log(2) / 24),
  f = c(12, 18, 24),
  d = c(0, 0.2, 0.4),
  a_k = c(0, 3, 6),
  omega_k = c(15, 20, 25),
  r = c(2, 3),
  target_re = c(0.9, 1, 1.1),
  metric = "n",
  alpha = 0.025,
  power = 0.9,
  time_unit = "months",
  formula = "Schoenfeld",
  verbose = TRUE
)
head(subset(results, status == "Success"))
}

}
\seealso{
\code{\link{find_rho_for_target_re}} for single parameter set
  optimization, \code{\link{calc_relative_efficiency_grid}} for grid-wide
  relative efficiency analysis.
}
