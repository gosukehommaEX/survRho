% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_rho_for_target_re.R
\name{find_rho_for_target_re}
\alias{find_rho_for_target_re}
\title{Find Accrual Rate Multiplier for a Target Relative Efficiency}
\usage{
find_rho_for_target_re(
  HR,
  hazard_c,
  a_k,
  omega_k,
  f,
  d_t,
  d_c,
  r,
  alpha,
  power,
  time_unit = "months",
  metric = "n",
  target_re = 1,
  rho_range = c(0.1, 5),
  tolerance = 1e-06,
  max_iter = 1000,
  formula = "Schoenfeld"
)
}
\arguments{
\item{HR}{A hazard ratio under the alternative hypothesis.}

\item{hazard_c}{A hazard rate for the control group (per time unit).}

\item{a_k}{A numeric vector of accrual time interval boundaries (in the
specified time unit).}

\item{omega_k}{A numeric vector of accrual intensities (patients per time
unit) for each accrual interval.}

\item{f}{A follow-up time after completion of accrual.}

\item{d_t}{A dropout rate for the experimental group (annual probability).}

\item{d_c}{A dropout rate for the control group (annual probability).}

\item{r}{A scalar allocation ratio to the experimental group (T:C = r:1).}

\item{alpha}{A one-sided level of significance.}

\item{power}{A target power.}

\item{time_unit}{A character string specifying the time unit. One of
"months" (default), "weeks", or "years".}

\item{metric}{A character string specifying the outcome for which the
target RE is sought. One of "n" (total sample size),
"accrual_duration" (accrual duration a_K), or "tau" (total trial
duration). Default is "n".}

\item{target_re}{A target relative efficiency value. Default is 1.}

\item{rho_range}{A numeric vector of length 2 giving the search range for
rho. Default is c(0.1, 5).}

\item{tolerance}{A numeric value specifying the tolerance for the root
finder. Default is 1e-6.}

\item{max_iter}{A maximum number of iterations for the root finder.
Default is 1000.}

\item{formula}{A character string specifying the formula used to calculate
the required number of events. One of "Schoenfeld" (default) or "Freedman".}
}
\value{
An object of class \code{"rho_search"} inheriting from
  \code{"list"}, with components:
\describe{
  \item{rho}{The rho value at which RE equals target_re.}
  \item{re}{The relative efficiency achieved at the returned rho.}
  \item{target_re}{The target relative efficiency.}
  \item{metric}{The outcome used for optimization.}
  \item{r}{The allocation ratio used.}
  \item{converged}{Logical indicating whether the root finder converged.}
  \item{objective}{The absolute difference |re - target_re| at the returned rho.}
  \item{iterations}{The number of iterations used by the root finder.}
}
When printed, a human-readable summary is shown; see
\code{\link{print.rho_search}}. The list components remain accessible in
the usual way (e.g., \code{result$rho}).
}
\description{
Finds the value of rho (accrual rate multiplier) at which the relative
efficiency (RE) of r:1 randomization compared to 1:1 randomization equals
a specified target value for a chosen outcome. The root is found with
\code{\link[stats]{uniroot}} (Brent's method).
}
\details{
The relative efficiency is defined as \code{RE = value_{r = 1} / value_{r}} for
the chosen outcome. The function solves RE(rho) - target_re = 0 using
\code{\link[stats]{uniroot}}. Before the root search, the function checks
whether target_re lies within the interval [RE(rho_range[1]), RE(rho_range[2])]
and issues an informative error message if not.
}
\note{
This function requires \code{\link{calc_relative_efficiency}}.
}
\examples{
\dontrun{
result <- find_rho_for_target_re(
  HR = 0.7,
  hazard_c = log(2) / 12,
  a_k = c(0, 3, 6),
  omega_k = c(15, 20, 25),
  f = 12,
  d_t = 0.3,
  d_c = 0.3,
  r = 2,
  alpha = 0.025,
  power = 0.9,
  metric = "tau",
  target_re = 1,
  formula = "Schoenfeld"
)
print(result)
result$rho
}

}
\seealso{
\code{\link{calc_relative_efficiency}} for the underlying
  relative efficiency calculation; \code{\link{print.rho_search}} for the
  print method.
}
