% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_relative_efficiency_grid.R
\name{calc_relative_efficiency_grid}
\alias{calc_relative_efficiency_grid}
\title{Calculate Relative Efficiency Over a Grid of Parameter Combinations}
\usage{
calc_relative_efficiency_grid(
  HR,
  hazard_c,
  f,
  d,
  a_k,
  omega_k,
  r,
  rho,
  alpha,
  power,
  time_unit = "months",
  formula = "Schoenfeld",
  verbose = FALSE
)
}
\arguments{
\item{HR}{A numeric vector of hazard ratios under the alternative hypothesis.}

\item{hazard_c}{A numeric vector of hazard rates for the control group
(per time unit).}

\item{f}{A numeric vector of follow-up times after completion of accrual.}

\item{d}{A numeric vector of dropout rates (annual probability) applied to
both the experimental and control groups (d_t = d_c = d).}

\item{a_k}{A numeric vector of accrual time interval boundaries.}

\item{omega_k}{A numeric vector of accrual intensities (patients per time
unit) for each accrual interval.}

\item{r}{A numeric vector of allocation ratios to the experimental group
(T:C = r:1).}

\item{rho}{A numeric vector of accrual rate multipliers, the same length
as r.}

\item{alpha}{A one-sided level of significance.}

\item{power}{A target power.}

\item{time_unit}{A character string specifying the time unit. One of
"months" (default), "weeks", or "years".}

\item{formula}{A character string specifying the formula used to calculate
the required number of events. One of "Schoenfeld" (default) or "Freedman".}

\item{verbose}{Logical. If TRUE, progress messages are printed for grids
with more than 50 combinations. Default is FALSE.}
}
\value{
An object of class \code{"re_grid"} inheriting from \code{"list"},
  with components:
\describe{
  \item{original_df}{Full output of calc_trial_duration() for every
    parameter combination, including the baseline (r = 1) case. Columns
    HR, hazard_c, f, and d are appended for identification.}
  \item{re_df}{Relative efficiency results with one row per (HR, hazard_c,
    f, d, r, rho) combination. Columns include HR, hazard_c, mst_c
    (= log(2) / hazard_c), f, d, r, rho, re_n, re_accrual_duration, and
    re_tau.}
}
When printed, a compact summary of the grid is shown; see
\code{\link{print.re_grid}}. The list components remain accessible in the
usual way.
}
\description{
Evaluates the relative efficiency of r:1 randomization compared to 1:1
randomization over all combinations of hazard ratios, control-group hazard
rates, follow-up periods, and common dropout rates. For each parameter
combination, \code{\link{calc_relative_efficiency}} is called with the
specified allocation ratios and accrual rate multipliers.
}
\details{
The total number of scenarios evaluated is
length(HR) x length(hazard_c) x length(f) x length(d) x (1 + length(r)),
where "1 +" accounts for the baseline r = 1 case in each combination.

The relative efficiency for outcome x is
\code{RE_x = value_{r = 1} / value_{r}},
so that RE_x > 1 indicates r:1 randomization is more efficient than 1:1.
}
\note{
This function requires \code{\link{calc_relative_efficiency}} and
  the \code{dplyr} package.
}
\examples{
\dontrun{
results <- calc_relative_efficiency_grid(
  HR = seq(0.6, 0.9, by = 0.1),
  hazard_c = c(log(2) / 6, log(2) / 12, log(2) / 24),
  f = c(12, 18, 24),
  d = c(0, 0.2, 0.4),
  a_k = c(0, 3, 6),
  omega_k = c(15, 20, 25),
  r = c(2, 3),
  rho = c(1.2, 1.3),
  alpha = 0.025,
  power = 0.9,
  time_unit = "months",
  formula = "Schoenfeld",
  verbose = TRUE
)
print(results)
}

}
\seealso{
\code{\link{calc_relative_efficiency}} for single parameter set
  analysis; \code{\link{print.re_grid}} for
  display.
}
